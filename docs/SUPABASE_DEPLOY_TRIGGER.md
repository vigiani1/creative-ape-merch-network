# Supabase deployment trigger

This no-op documentation commit was added to create a fresh `main` branch event after connecting the repository to the Supabase GitHub integration.

It does not change the application schema or runtime behavior.
